<h3 class="card-title text-center"><%= t('activerecord.attributes.contact.update_success') %></h3>

  <p class="text-center mt-5"><%= t('feedback.update_success') %></p>

<div class="row">
  <div class="d-flex ">
    <%= link_to t('helpers.back'), root_path, class: 'btn btn-secondary mt-5' %>
  </div>
</div>
