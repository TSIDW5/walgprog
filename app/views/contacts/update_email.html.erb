<h3 class="card-title"><%= t('activerecord.attributes.contact.email_exists') %></h3>

<p class="text-center mt-5"><%= t('feedback.email_exists') %></p>

<div class="row">
  <div class="d-flex ">
    <%= link_to t('helpers.back'), new_contact_path, class: 'btn btn-secondary mt-5' %>
  </div>
</div>
