<div class="card">
  <div class="card-header">

  </div>
  <div class="card-body">
    <div class="row">

    </div>
  </div>
  <div class="row">
    <div class="d-flex ">
      <%= link_to t('helpers.back'), new_contact_path, class: 'btn btn-secondary float-left' %>
    </div>
  </div>
</div>

<h3 class="card-title"><%= t('activerecord.attributes.contact.token_expired') %></h3>

<p class="text-center mt-5"><%= t('feedback.time_exceeded') %></p>

<div class="row">
  <div class="d-flex ">
    <%= link_to t('helpers.back'), new_contact_path, class: 'btn btn-secondary mt-5' %>
  </div>
</div>
